MODEL=o4-mini
OPENROUTER_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_CHAT_IDS=
TELEGRAM_POLLING_TIMEOUT_SEC=25
AUDIT_LOG_PATH=/home/jump/jarvis1net/logs/audit.jsonl
MCP_SERVER_URL=https://mcp.jarvis1.net
MCP_API_KEY=
MCP_TIMEOUT_SEC=15
MCP_MAX_TOOL_ROUNDS=10
SESSION_CONTEXT_PATH=

# Delegated Microsoft Graph access token for this agent instance (optional). Sent as X-Graph-Authorization to MCP for microsoft_* tools.
# Prefer short-lived tokens; rotate via your own OAuth/device flow on the agent host — do not share one VPS operator token across users.
MICROSOFT_GRAPH_ACCESS_TOKEN=

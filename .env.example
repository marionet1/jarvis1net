# Secrets only. Non-secret runtime settings live in config/runtime_config.json.
OPENROUTER_API_KEY=
TELEGRAM_BOT_TOKEN=

# Optional Microsoft Graph secret token (runtime secret override for MCP tools).
MCP_GRAPH_ACCESS_TOKEN=
